//js代码
var message = "Hello World";
var name = "master-lu";

function sum(num1, num2) {
  return num1 + num2
}
function test() {
  return "test function"
}
//commomjs的模块化导出写法
module.exports = {
  message: message,
  name: name,
  sum: sum,
  test: test
}